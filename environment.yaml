name: suicide_forecast
channels:
    - conda-forge
    - defaults
dependencies:
    - python=3.10
    - pandas
    - numpy
    - matplotlib
    - plotly          # src/utils/viz.py
    - scikit-learn    # src/geospatial/st_dbscan.py
    - lightgbm        # darts LightGBMModel, notebooks/0.1
    - python-dotenv
    - requests
    - pyyaml
    - ipykernel
    - python-levenshtein
    - ipywidgets
    - pytest          # tests/
    - pip
    - pip:
        - uszipcode
        - "sqlalchemy_mate<2.0.0"
        - redivis
        - darts
